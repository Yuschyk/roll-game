import React, { FC } from 'react';

import { InnerWrapper, OuterWrapper } from './styled';


export const PreviousRolls:FC = ():JSX.Element => (
  <OuterWrapper>
    <InnerWrapper>
      123
    </InnerWrapper>
  </OuterWrapper>
);
